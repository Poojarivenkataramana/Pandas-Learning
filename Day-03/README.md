# 🐼 Pandas Day 3 – Data Filtering & Conditional Selection

## 📚 Topics Covered

- Filtering Rows
- Comparison Operators
- Logical Operators (`&`, `|`, `~`)
- `isin()`
- `between()`
- `str.contains()`
- `str.startswith()`
- `str.endswith()`
- Common Interview Questions

---

## 📖 What I Learned

Today I learned how to filter data efficiently in Pandas using conditions and logical operators. I also explored different string filtering methods and techniques to retrieve specific records from a dataset.

---

## 🔍 Filtering Methods Covered

### Comparison Operators
- `==`
- `!=`
- `>`
- `<`
- `>=`
- `<=`

### Logical Operators
- `&` (AND)
- `|` (OR)
- `~` (NOT)

### Useful Pandas Functions
- `isin()`
- `between()`
- `str.contains()`
- `str.startswith()`
- `str.endswith()`

---

## 📂 Dataset Used

- `big_employee_data_cleaned.csv`

---

## 🎯 Day 3 Summary

This session focused on filtering and selecting data using different conditions. These concepts are essential for data analysis and are widely used in real-world projects.
